import { Box, Drawer, IconButton, Image, Input, InputGroup, Portal, Stack, Text } from "@chakra-ui/react"

const StaffDashboard = () => {
  return (
    <Stack bgColor='primary' height='100vh'>
      <Text> Hi</Text>
    </Stack>
  )
}

export default StaffDashboard